<div class="opaque-overlay">
    <div class="container p-4">
      
        <div class="row setup-content">
          <div class="col-lg-12 well text-center"> 
            <div class="search-content">
              <hgroup class="mb20">
          		<h1 class="text-primary">Resultado da Busca</h1>
          		<h2 class="lead"><strong class="text-danger"><%= @profiles.count%></strong> resultado encontrado(s) para busca por <strong class="text-danger"><%= params[:search] %></strong></h2>								
          	</hgroup>
        
            <section class="col-xs-12 col-sm-6 col-md-12">
              <% if @profiles.count.zero? %>
                Sua busca não retornou resultados.
              <% else %>
              <% @profiles.each do |profile| %>
                <article class="search-result row">
            			<div class="col-xs-12 col-sm-12 col-md-4">
            				<%= image_tag profile.image.url(:thumb), class: "img-responsive" %>
            			</div>
            			<div class="col-xs-12 col-sm-12 col-md-3">
            				<ul class="meta-search">
            					<li><i class="fa fa-cutlery" aria-hidden="true"></i> <span><%= profile.category %></span></li>
            					<li><i class="fa fa-whatsapp" aria-hidden="true"></i> <span><%= profile.phone %></span></li>
            				</ul>
            			</div>
            			<div class="col-xs-12 col-sm-12 col-md-5 excerpet">
            				<h3><a href="/profiles/<%= profile.id %>" title=""><%= profile.business_name %></a></h3>
            				<p><%= profile.description %></p>						
            			</div>
            			<span class="clearfix borda"></span>
            			</article>
            			<% end %>
            			<% end %>
            		
            </section>
              
            </div>
          </div>
        </div>
      
    </div>
</div>

  <div class="container p-4">
    
    
  </div>

